# Playa del Carmen Guide - Environment Variables Template
# Copy this file to .env and fill in your actual values
# NEVER commit .env files to git!

# Widget (React/Vite) - Public Mapbox token
# This will be exposed in browser code, so create a RESTRICTED token:
# - In Mapbox dashboard, restrict to your widget URL
# - Scopes: Only "Styles:Read"
VITE_MAPBOX_ACCESS_TOKEN=pk.your_public_token_here

# MCP Server (Cloudflare Workers) - Secret token (if needed for backend)
# Set via: wrangler secret put MAPBOX_ACCESS_TOKEN
# For local dev, create mcp-server/.dev.vars with:
# MAPBOX_ACCESS_TOKEN=sk.your_secret_token_here
