{
  "name": "priority-inbox-mcp-server",
  "version": "1.0.0",
  "description": "MCP Server for Priority Inbox widget",
  "main": "dist/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "build": "tsc",
    "tail": "wrangler tail"
  },
  "dependencies": {},
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250110.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.95.0"
  }
}
