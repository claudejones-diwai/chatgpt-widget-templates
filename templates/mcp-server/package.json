{
  "name": "{{PROJECT_NAME}}-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for {{WIDGET_NAME}}",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241022.0",
    "typescript": "^5.9.2",
    "wrangler": "^3.78.0"
  }
}
